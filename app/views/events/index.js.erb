$('#events').prepend('<%= j render(@events) %>');
<% @events.each do |event| %>
  $('#<%= j escape_dot event.event_id %>').effect("highlight", {}, 1500);
<% end %>
$('.event').filter(function(i) { return i >= <%= num_events %>; }).remove();
EventPoller.poll();
